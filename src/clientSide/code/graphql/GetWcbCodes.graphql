query wcbCodes($ctx: ServiceContextInput!) {
    wcbCodes(ctx: $ctx) {
        wcbCodeId
        geoStateId
        countryCode
        xrefCode
		shortName
        stateCode
		wcbCodeCode
    }
}
