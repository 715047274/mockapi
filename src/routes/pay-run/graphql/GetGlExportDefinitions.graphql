query glExportDefinitions(
    $ctx: ServiceContextInput
    $payGroupId: Int!
  ) {
    glExportDefinitions(
    ctx: $ctx
    payGroupId: $payGroupId) {
        dataExportDefinitionId
        shortName
    }
  }