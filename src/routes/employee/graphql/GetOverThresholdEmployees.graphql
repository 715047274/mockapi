query previewOverThresholdEmployees(
    $payRunId: Long!
    $toComparePayRunId: Long
    $take: Int
) {
    previewOverThresholdEmployees(
        payRunId: $payRunId
        toComparePayRunId: $toComparePayRunId
        take: $take
    ) {
        totalCount
        employeeIds
    }
}
