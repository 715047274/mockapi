query payrollReasons($ctx: ServiceContextInput!) {
    payrollReasons(ctx: $ctx){
        reasonId
        shortName
        countryIds
        scopeIds
        scopes{
            reasonScopeId
            xRefCode
        }
    }
}
