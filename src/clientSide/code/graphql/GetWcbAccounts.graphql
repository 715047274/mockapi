query wcbAccounts($ctx: ServiceContextInput!) {
    wcbAccounts(ctx: $ctx) {
        wcbAccountId
        legalEntityId
        effectiveStart
        effectiveEnd
		shortName
        legalEntityName
		isTimeBased
    }
}
