query previousPayRuns(
  $ctx: ServiceContextInput!
  $payRunId: Long!
) {
  previousPayRuns(
    ctx: $ctx
    payRunId: $payRunId
    order: [{ payDate: DESC}]
    skip: 0
    take: 1
  ) {
      totalCount
      items
      {
        payRunId
      }
  }
}