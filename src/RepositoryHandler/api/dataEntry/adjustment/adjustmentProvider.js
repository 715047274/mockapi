 import {mockDataOptions} from "./adjustmentProviderOptions.js";

const getAllMockAdjustments = (payRunId) =>{
    const {
        payEntryAdjustmentBatchId,
        employmentTypes,
        adjustmentTypes,
        codes,
        savedBy,
        lastUpdated,
        lastUpdatedTime,
        employeePriorStateBundle,
        importSets,
        laborMetrics,
    } = mockDataOptions;
}