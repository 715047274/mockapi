query payRunProgress($payRunId: Long!) {
    payRunProgress(payRunId: $payRunId) {
        payRunProcessState
        payrollCommitted
        payrollCommittedDate
        payRunBlocked
        remainingTime
        lastUpdateTimestamp
        currentTimestamp
    }
}
