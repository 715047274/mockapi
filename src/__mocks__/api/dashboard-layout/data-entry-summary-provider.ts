import { DataEntrySummary } from '@models/data-entry/DataEntrySummaryType';
import { DataEntrySummaryMockData } from './data-entry-summary-options';

export const getDataEntrySummaryMock = (): DataEntrySummary => {
    return DataEntrySummaryMockData;
};
