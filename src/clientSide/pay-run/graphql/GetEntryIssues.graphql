query entryIssues(
    $ctx: ServiceContextInput!
    $filters: [FilterModelInput]
) {
  entryIssues(
    ctx: $ctx
    filters: $filters
  ) {
    payRunMessageId
    payRunMessageLevelId
    payEntryBatchDataId
    payEntryBatchDataStageId
    messageTitle
    messageLabel
    messageParams
  }
}