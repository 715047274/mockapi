scalar Long

type Query {
    payRunProgress(payRunId: Long!): PayRunProgress!
}

type PayRunProgress {
    payRunProcessState: String!
    payrollCommitted: Boolean!
    payrollCommittedDate: String
    payRunBlocked: Boolean!
    remainingTime: Int
    lastUpdateTimestamp: String
    currentTimestamp: String!
}
